name: C/C++ CI

on:
  push:
    branches: [ "main" ]

jobs:
  setup_qemu:
    runs-on: ubuntu-latest
    steps:
      - 
        name: install qemu
        run: |
          apt update && apt install qemu
