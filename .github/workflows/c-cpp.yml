name: C/C++ CI

on:
  pull_request:
    branches: [ "yiyang" ]
  push:
    branches: ["yiyang"]

jobs:
  test-wasm:
    runs-on: ubuntu-latest
    steps:
      - 
        name: install build essentials
        run: 
          sudo apt update && 
          sudo apt install nodejs
      
      -
        uses: actions/checkout@v3
      - 
        name: install mocha
        run: |
          npm install -g mocha
      -
        name: run test
        run: |
          npx run wasm.test.js
